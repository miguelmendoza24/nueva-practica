/*function squareSum(numbers) {
  let suma = 0;
  for (let index = 0; index < numbers.length; index++) {
   // console.log(index);
    
    let cuadrado = numbers[index] * numbers[index];
   // console.log(cuadrado);
    
    suma = suma + cuadrado; 
  }
  return suma;
}
console.log(squareSum([1,2,2]))
//Your order, please
//Your order, please*/
